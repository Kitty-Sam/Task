import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    root: {
        alignItems: 'center',
        justifyContent: 'space-around',
        flex: 1,
    },
    image: {
        width: '95%',
        height: '50%',
    },
});
