import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    root: {
        flex: 1,
        margin: 18,
    },
    titleContainer: {
        flexDirection: 'row',
        justifyContent: 'flex-end',
    },
    boldText: {
        fontWeight: '700',
    },
});
